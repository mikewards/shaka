[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["shaka-api/**", "scripts/**", "data/**", "Dockerfile", "railway.toml"]

[deploy]
healthcheckPath = "/v1/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
